# 배치 러너는 validation·analysis 서비스를 직접 import 한다(HTTP 서버는 불필요).
# torch 는 Dockerfile 에서 CPU 빌드로 별도 설치한다.
pydantic==2.7.4
pydantic-settings==2.3.4
kiwipiepy==0.23.2
transformers==4.44.2
# S3 게시글별 .json 입출력(run_validation). pure-python, torch처럼 이미지 비대화 없음.
boto3==1.34.144
# 배치 작업 집합(Redis Set)·비용 카운터 접근(run_bedrock, PIPE-2SB-26/61/72/73).
# pure-python. run_bedrock 은 이 패키지를 지연 import 하므로(s3_io의 boto3와 같은 이유)
# 로컬 .venv 에 없어도 다른 러너의 import 는 깨지지 않는다.
redis==5.0.4
