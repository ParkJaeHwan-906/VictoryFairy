# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/

# --- 가상환경 ---
.venv/
venv/
env/

# --- 환경변수 (실제 .env 는 제외, 템플릿 .env.example 만 추적) ---
.env
.env*
!.env.example

# --- 로컬 전용: 스테이지 간 파일 저장소 ---
# 루트 앵커(/)로 최상위 data/ 만 제외한다.
# (모듈 사전 analysis/core/data, validation/core/data 는 커밋되어야 하므로 무시하면 안 됨)
/data/

# # --- 로컬 전용: 배치 러너 ---
# /pipeline/

# --- Claude 개인 설정 (팀 공유용 settings.json·harness 는 커밋) ---
.claude/settings.local.json

# --- OS / 에디터 ---
.DS_Store
*.swp
.idea/
.vscode/
