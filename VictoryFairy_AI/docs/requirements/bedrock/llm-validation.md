# Bedrock LLM 2차 검열 요구사항
> 상태: **승인됨 (2026-07-25)** · 모듈: bedrock (신규) · 최종 수정: 2026-07-25
> 함께 승인된 문서: `docs/requirements/pipeline/two-stage-batch.md` · `docs/requirements/pipeline/s3-io.md`(v2)
> — **세 문서는 한 묶음이다.**
>
> ⚠️ **판정 목표치(재현율·폐기율)는 아직 잠정치다.** 승인된 것은 **케이스 클래스와 확정 절차**(BRK-LLM-46/47)이며,
> 숫자 자체는 shadow 실측·`test-data` 세트 확정 뒤에 정해진다. **잠정 숫자를 달성 기준으로 삼아 구현을
> 평가하지 마라.**

## 배경 / 목적
사전·정규식 검열(`validation` 모듈)은 **문맥 의존 비하·사전에 없는 신조 표기**를 구조적으로 놓치고,
**스팸·광고·주제 무관 글은 애초에 판정 대상이 아니다**(사전엔 그런 개념이 없다).
반대로 사전은 오탐이 거의 없다(자연스러운 한국어 30문장 측정 기준 신규 오탐 미발생 — `docs/modules/validation.md`).
즉 남은 손실은 대부분 **미탐** 쪽이고, 그 미탐분을 LLM 이 2차로 잡는다.

> ⚠️ **이 단계는 통과율을 깎는 방향으로만 작동한다.** 입력이 이미 "사전이 통과시킨 글"이므로, 여기서
> 판정을 세게 잡으면 **정상 문장이 폐기되는 손실이 그대로 순증**한다. 그래서 이 문서의 재현율 요구사항에는
> 전부 짝이 되는 오탐 요구사항이 붙어 있다.

## 판정 축 (2개)
| 축 | 식별자 | 무엇을 거르나 | 오탐 위험 |
|---|---|---|---|
| A | `abuse` | 욕설·비속어·인신공격·혐오 — 사전이 놓친 우회 표기·문맥 의존 표현 | 중 |
| B | `spam` / `offtopic` | 스팸·광고성(`spam`) · 야구/야구 인물과 전혀 무관한 내용(`offtopic`) | **높음 (구조적)** |

> **축 B 는 축 A 보다 오탐 위험이 구조적으로 크다.** 커뮤니티 글에는 원래 잡담·밈·경기 외 수다가 섞이고,
> DCInside·FMKorea 는 특히 그렇다. "야구와 무관"의 경계가 모호해서 **정상적인 커뮤니티 잡담을 광고·무관으로
> 오판**하기 쉽다. 그래서 축 B 는 (1) `spam` 과 `offtopic` 을 **별도 식별자로 분리**하고, (2) `offtopic` 의
> 적용 범위를 **게시글 본문으로 한정**하며(BRK-LLM-19), (3) 오탐 케이스를 축 A 보다 촘촘히 박았다(BRK-LLM-40~45).

## 범위
- 포함:
  - `bedrock/` 모듈의 판정 서비스 계약 — 텍스트 in / `{is_valid, axis, message}` out.
  - Bedrock 호출 설정(추론 프로파일 ID·리전·타임아웃·재시도·결정성 파라미터·데이터 리전 제약).
  - **단위 종류(본문/댓글) 인자** — `offtopic` 의 적용 범위를 가르는 근거.
  - 목표치 **확정 절차**(지금 숫자는 전부 잠정치다)와 그 실측 수단(`BEDROCK_SHADOW`).
  - 응답 파싱·판정 실패 시 폴백 계약.
  - **판정 요구사항** — 축 A·B 각각의 "잡아야 하는 것 / 잡히면 안 되는 것" + 목표치.
- 제외 (의도적):
  - **프롬프트 문안 자체** — `accuracy-tuner` 소관. 이 문서는 "무엇이 참이어야 하는가"만 정한다.
  - **S3 키·러너 배선·트리거·스케줄·처리 순서(게시글 단위)·비용 상한·프롬프트 캐싱** → `docs/requirements/pipeline/two-stage-batch.md`.
  - **패턴 검열(사전·정규식) 변경** — 이번 범위 아님. 사전에 단어를 추가해 푸는 것은 `dict-curator`의 별건.
  - **폐기된 글의 사후 검수 UI/재심** — 미도입.
  - **광고 판정의 외부 신호 활용**(URL 블랙리스트·도메인 평판 등) — 이번엔 텍스트 판정만.
  - **analysis(NER) 단계와의 연결** — 다음 이터레이션.

---

## 결정적 계약 (EARS)

### A. 모듈 경계 · 인터페이스

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| BRK-LLM-1 | 유비쿼터스 | THE 시스템 SHALL 판정 서비스를 **`{text, unit_kind}` 항목의 목록**(`judge(items: list[{text, unit_kind ∈ "body"·"comment"}])`)을 받아 **입력과 같은 길이의 결과 목록**(`{is_valid, axis, message, fallback}`)을 반환하는 형태로 제공한다 | `is_valid` 는 `ValidationResponse` 와 동일 의미 — 러너의 라우팅 코드는 `is_valid` 만 본다(PIPE-2SB-16). **`unit_kind` 는 항목별 인자다**: 게시글 단위 순차 처리(PIPE-2SB-8)로 한 배치에 **본문 1 + 댓글 N 이 섞여 들어오므로**, 배치 단위 인자로는 표현할 수 없다. `BRK-LLM-19`(`offtopic` 본문 한정)가 살아 있는 한 이 인자는 **필수**이며, 없으면 구현이 불가능하다. 열거형 값이지 S3 키가 아니므로 BRK-LLM-2 와 충돌하지 않는다 |
| BRK-LLM-1b | 유비쿼터스 | THE 시스템 SHALL `axis` 값을 `"abuse"` · `"spam"` · `"offtopic"` 중 하나로 두고, 통과(`is_valid=true`)일 때는 `null` 로 둔다 | 열린 문자열 금지. `accuracy-tuner` 가 축별로 오탐·재현율을 **분리 측정**할 수 있어야 한다 |
| BRK-LLM-1c | 예외 | IF 한 단위가 두 축 모두에 해당하면(예: 욕설이 섞인 광고 글), THEN THE 시스템 SHALL `axis` 를 **`abuse` 우선**으로 기록한다 | 축이 하나만 기록되는 스키마이므로 우선순위를 고정한다. 이유: 욕설 폐기가 광고 폐기보다 근거가 명확해 사후 재심 판단이 쉽다. ⚠️ **이 우선순위는 이제 폐기 범위까지 결정한다** — 본문이 `abuse` 로 기록되면 게시글 전체가 fail 이고(PIPE-2SB-11b), `spam`/`offtopic` 이면 본문 단위만 폐기된다(PIPE-2SB-11c). 즉 **겹친 본문은 더 넓은 쪽(전체 fail)으로 라우팅된다 — 의도된 동작이다** |
| BRK-LLM-2 | 유비쿼터스 | THE 시스템 SHALL `bedrock` 모듈에서 S3·버킷·키 규약을 참조하지 않는다 | `bedrock/` 안에 `boto3.client("s3")`·`S3_BUCKET` 없음(PIPE-2SB-15). 텍스트 in / 판정 out |
| BRK-LLM-3 | 유비쿼터스 | THE 시스템 SHALL `bedrock` 모듈에서 validation 모듈의 사전·정규식을 사용하지 않는다 | 2차 검열은 독립 판정. 사전 재적용은 중복이며, 사전이 이미 통과시킨 입력이다 |
| BRK-LLM-4 | 이벤트 | WHEN 판정이 폐기로 나오면, THE 시스템 SHALL `message` 에 **사람이 읽을 수 있는 폐기 사유**를 담는다 | `message` 예: `문맥상 특정 인물 인신공격` · `상품 판매 유도 광고`. 빈 문자열·모델 원문 덤프 금지 |
| BRK-LLM-19 | 유비쿼터스 | THE 시스템 SHALL `offtopic` 판정을 **본문 단위(`unit_kind="body"`)에만** 적용하고 댓글 단위에는 적용하지 않는다 | 댓글은 짧고 부모 글 맥락에 의존해(`ㅋㅋㅋ`·`이건 좀`) 단독으로 주제 관련성을 판정할 수 없다. **`spam`(광고 댓글)과 `abuse` 는 댓글에도 적용한다.** ⚠️ **빈 본문 글의 `title` 은 `unit_kind="body"` 로 들어오므로(PIPE-2SB-8b) `offtopic` 적용 대상이다** — 제목은 그 글의 주제를 가장 직접적으로 드러내는 텍스트라 오히려 판정 근거가 좋다. ← 미해결 질문 1 |

### B. 모델 호출 설정

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| BRK-LLM-5 | 유비쿼터스 | THE 시스템 SHALL 모델 식별자를 **단일 환경변수** `BEDROCK_MODEL_ID`**(가정 키명)** 에서 읽고 코드에 하드코딩하지 않는다 | `.env.example` 에는 키 이름/플레이스홀더만. **본문·댓글이 같은 모델을 쓴다** |
| BRK-LLM-6 | 유비쿼터스 | THE 시스템 SHALL 모델 식별자로 **`apac.*` Claude Sonnet 4 추론 프로파일 ID** 하나를 사용한다 | 베어 모델 ID 를 조립하지 않는다. 근거: **Sonnet 4 는 `INFERENCE_PROFILE` 전용**이라 베어 모델 ID 호출이 실제로 실패한다. 단위 종류에 따른 모델 분기는 **두지 않는다** — 실측상 전수 처리해도 하루 약 $13 이라 저가 모델로 나눌 비용 근거가 없고, 나누면 본문·댓글 판정 기준이 갈려 지표만 오염된다 |
| BRK-LLM-6b | 유비쿼터스 | THE 시스템 SHALL `global.*` 추론 프로파일을 사용하지 않는다 | **데이터가 APAC 밖으로 나가지 않아야 한다**는 것이 `apac.*` 선택의 이유(사용자 확정). 설정값이 `global.` 로 시작하면 기동 시 거부하고 중단한다 |
| BRK-LLM-6c | 유비쿼터스 | THE 시스템 SHALL `apac.*` 프로파일의 처리 위치를 **"APAC 이내"로만 보장하고 "한국 이내"로 보장하지 않는다** | `apac.*` 는 **APAC 8개 리전**(도쿄·서울·오사카·뭄바이·하이데라바드·싱가포르·시드니·멜버른)으로 라우팅된다 — **서울 고정이 아니다.** 국내 처리(데이터 국외 이전 금지)가 요건이 되면 `apac.*` 로는 충족할 수 없고 프로파일 전략을 다시 정해야 한다 |
| BRK-LLM-7 | 유비쿼터스 | THE 시스템 SHALL Bedrock 리전을 `BEDROCK_REGION`(기본 `ap-northeast-2`)에서 읽는다 | S3 리전 설정과 별도 키 — 모델 가용 리전이 다를 수 있다 |
| BRK-LLM-8 | 유비쿼터스 | THE 시스템 SHALL 호출 파라미터를 결정성 우선으로 고정한다(temperature 0, top_p 고정, `max_tokens` 상한) | 같은 입력 재실행 시 판정이 요동치지 않아야 `accuracy-tuner` 가 측정할 수 있다. **완전 재현은 보장하지 않는다** |
| BRK-LLM-9 | 유비쿼터스 | THE 시스템 SHALL 단일 호출 타임아웃을 **읽기 30초 / 연결 5초(가정)** 로 둔다 | NAT(단일 AZ 2a) 경유라 지연이 붙는다. 무한 대기 금지 |
| BRK-LLM-10 | 유비쿼터스 | THE 시스템 SHALL 판정 대상 텍스트가 **4000자(가정)** 를 넘으면 앞 4000자만 판정에 사용하고 절단 사실을 로그로 남긴다 | 초장문 게시글의 토큰 폭주 방지. 절단분 미판정은 알려진 한계 |

### C. 응답 파싱 · 실패 처리

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| BRK-LLM-11 | 유비쿼터스 | THE 시스템 SHALL 모델 응답을 **지정된 JSON 스키마로만** 파싱한다 | 자유 텍스트에서 "폐기"/"통과" 같은 단어를 찾아 해석하지 않는다 |
| BRK-LLM-11b | 예외 | IF 응답의 `axis` 값이 정의된 세 값(`abuse`·`spam`·`offtopic`) 중 하나가 아니면, THEN THE 시스템 SHALL 그 응답을 스키마 불일치로 간주한다(BRK-LLM-13 적용) | 모델이 지어낸 축 이름을 산출물에 그대로 흘리지 않는다 |
| BRK-LLM-12 | 이벤트 | WHEN 한 호출에 여러 단위를 함께 보내면, THE 시스템 SHALL 응답 항목을 요청 순서와 **1:1 인덱스로** 대응시킨다 | 단위 3개 요청 → 결과 3개, 순서 보존 |
| BRK-LLM-13 | 예외 | IF 응답 항목 수가 요청 단위 수와 다르거나 스키마가 어긋나면, THEN THE 시스템 SHALL 그 호출을 실패로 간주하고 최대 **2회(가정)** 재시도한다 | 부분 응답을 억지로 짜맞추지 않는다 |
| BRK-LLM-14 | 예외 | IF `ThrottlingException`·`ServiceUnavailable`·타임아웃이면, THEN THE 시스템 SHALL 지수 백오프(1s·2s·4s, 가정)로 최대 **3회(가정)** 재시도한다 | 재시도 소진 시 BRK-LLM-15 적용 |
| BRK-LLM-15 | 예외 | IF 재시도를 모두 소진하고도 판정을 얻지 못하면, THEN THE 시스템 SHALL 그 단위를 **통과로 처리**하고 `message` 에 `LLM 판정 실패(폴백 통과)` 를 남긴다 | **fail-open 확정(사용자 결정).** 근거: 입력이 이미 사전 검열 통과분이라 폴백 통과는 "지금과 같은 상태" 유지이고, fail-closed 는 LLM 장애가 곧 대량 폐기가 된다 |
| BRK-LLM-15b | 이벤트 | WHEN 폴백 통과(BRK-LLM-15)로 판정을 대신하면, THE 시스템 SHALL 반환값의 `fallback` 을 `true` 로 둔다 | 러너가 이 값을 success 객체에 기록한다(PIPE-2SB-59). **폴백 통과는 `is_valid=true` 라 이 플래그가 없으면 어떤 산출물에도 흔적이 남지 않는다** — Bedrock 장애가 길었던 밤의 데이터가 "2차 검열을 거친 것"으로 오인된다 |
| BRK-LLM-16 | 선택 | WHERE `BEDROCK_DRY_RUN=true`**(가정 키명)** 인 경우, THE 시스템 SHALL 실제 모델 호출 없이 전건 통과로 판정하고 호출 예정 건수를 로그로 남긴다 | 비용 없이 배선·키 규약 검증. 테스트 기본값. **폐기율 측정에는 쓸 수 없다**(정의상 폐기 0) — 그 용도는 BRK-LLM-48 |
| BRK-LLM-16b | 선택 | WHERE `BEDROCK_DRY_RUN=true` 인 경우, THE 시스템 SHALL 전건 통과 결과의 `fallback` 을 **`true`** 로 두고 `message` 를 **`DRY_RUN(호출 없음)`** 으로 둔다 | DRY_RUN 산출물이 **"2차 검열을 통과한 데이터"로 오인되면 안 된다.** `fallback=true` 만으로는 **BRK-LLM-15(실제 Bedrock 장애로 인한 폴백 통과)와 산출물에서 구분되지 않으므로**, `message` 로 가른다 — `-15` 는 `LLM 판정 실패(폴백 통과)`, `-16b` 는 `DRY_RUN(호출 없음)`. 이 구분은 러너가 success 객체에 기록할 때(PIPE-2SB-59) 보존돼야 한다 |
| BRK-LLM-48 | 선택 | WHERE `BEDROCK_SHADOW=true`**(가정 키명)** 인 경우, THE 시스템 SHALL **실제로 모델을 호출해 판정**하되 그 결과를 success/failed 라우팅과 마커 기록에 쓰지 않고 `validation/bedrock/_shadow/...`(PIPE-2SB-70)에만 남긴다 | **폐기율 실측 전용 모드.** 조건 셋: (a) 비용 상한(PIPE-2SB-60)의 적용을 받는다, (b) **마커를 쓰지 않는다**(실제 판정이 아니므로 다음 정식 실행이 그 게시글을 그대로 처리한다), (c) 전건이 아니라 **표본**만 돌린다(BRK-LLM-35/44 의 500단위 규모면 충분하고 비용은 몇 달러) |
| BRK-LLM-48b | 예외 | IF `BEDROCK_DRY_RUN` 과 `BEDROCK_SHADOW` 가 동시에 켜지면, THEN THE 시스템 SHALL 기동을 거부하고 명확한 에러로 중단한다 | 두 모드는 양립할 수 없다(호출 안 함 ↔ 호출함). 조용히 한쪽을 이기게 두면 **비용이 0인 줄 알았는데 청구되는** 사고가 난다 |
| BRK-LLM-17 | 예외 | IF 자격증명이 없거나 `AccessDeniedException`(모델 접근 권한 미부여)·`ValidationException`(잘못된 프로파일 ID)이 발생하면, THEN THE 시스템 SHALL 즉시 명확한 에러로 중단하고 0이 아닌 종료 코드를 반환한다 | **구조적 실패에는 fail-open 을 적용하지 않는다** — 전건 폴백 통과로 조용히 끝나는 것이 최악 |
| BRK-LLM-18 | 이벤트 | WHEN 폐기 판정이 나오면, THE 시스템 SHALL 판정에 사용한 `modelId` 와 `promptVersion` 을 결과에 함께 실어 반환한다 | 러너가 failed 산출물에 기록(PIPE-2SB-13). 모델·프롬프트 교체 후 판정 변화 추적 |

---

## 판정 요구사항 (케이스 기반)
> **100% 가 없는 영역이다.** 아래는 전칭 명제가 아니라 **케이스 클래스 + 대표 케이스 + 목표치**다.
> "모든 우회 표기를 탐지한다" / "모든 광고를 거른다" 같은 문장은 이 문서에 없고, 앞으로도 쓰지 않는다.
>
> ⚠️ **대표 케이스의 전제 검증이 필요하다**: 아래 재현율 케이스는 "패턴 검열을 **통과**한 뒤 LLM 이 잡아야 하는 것"이어야
> 의미가 있다. 각 문장이 실제로 패턴 검열을 통과하는지는 `test-data` 가 실제 `validation/pattern/success/` 표본에서
> 확정한다(현재 표기는 **잠정**). 패턴이 이미 잡는 문장은 이 클래스의 대표 케이스가 될 수 없다.

### 축 A (`abuse`) — 재현율 · 잡아야 하는 것

| ID | 방향 | 요구사항 | 대표 케이스 (잠정) | 목표치 |
|---|---|---|---|---|
| BRK-LLM-20 | 재현율 | WHEN 입력이 **욕설 어휘 없이 특정 인물을 인격적으로 비하**하면, THE 시스템 SHALL 그 단위를 폐기한다 | `쟤는 사람 자체가 쓰레기라 야구도 저 모양이지` → 폐기 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-21 | 재현율 | WHEN 입력이 **집단(지역·성별·국적) 비하**를 담으면, THE 시스템 SHALL 그 단위를 폐기한다 | `거기 사람들은 원래 다 저 모양이야` → 폐기 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-22 | 재현율 | WHEN 입력이 **성적 대상화·성희롱 표현**을 담으면, THE 시스템 SHALL 그 단위를 폐기한다 | 대표 케이스는 `test-data` 가 실표본에서 확정 — 이 문서에 노골적 예문을 두지 않는다 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-23 | 재현율 | WHEN 입력이 **특정인에 대한 위협·폭력 선동**을 담으면, THE 시스템 SHALL 그 단위를 폐기한다 | `경기장 앞에서 기다린다 각오해라` → 폐기 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-24 | 재현율 | WHEN 입력이 **사전에 없는 신조·변형 비속어**를 문맥상 명백한 욕설로 사용하면, THE 시스템 SHALL 그 단위를 폐기한다 | 대표 케이스는 `test-data` 가 실제 패턴 통과분에서 추출해 확정 | **(잠정) 70% 이상** — 근거 없는 잠정치. 확정 절차는 BRK-LLM-47 |

### 축 B (`spam` / `offtopic`) — 재현율 · 잡아야 하는 것

| ID | 방향 | 요구사항 | 대표 케이스 (잠정) | 목표치 |
|---|---|---|---|---|
| BRK-LLM-25 | 재현율 | WHEN 입력이 **상품·서비스 판매나 외부 사이트 가입을 유도**하면, THE 시스템 SHALL 그 단위를 `spam` 으로 폐기한다 | `유니폼 정품 최저가 판매합니다 카톡 ○○○ 문의주세요` → 폐기 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-26 | 재현율 | WHEN 입력이 **도박·불법 홍보성 문구**를 담으면, THE 시스템 SHALL 그 단위를 `spam` 으로 폐기한다 | `첫충 30% 꽁머니 지급 지금 가입하세요` → 폐기 | 이 클래스 대표 케이스 전건 |
| BRK-LLM-27 | 재현율 | WHEN **게시글 본문**이 야구·야구 인물·야구 문화와 **전혀 관련이 없으면**, THE 시스템 SHALL 그 본문을 `offtopic` 으로 폐기한다 | `주식 종목 추천 좀 해주세요 요즘 뭐 사야 됨?` → 폐기 | **(잠정) 70% 이상** — 근거 없는 잠정치. 확정 절차는 BRK-LLM-47 |

### 축 A 오탐 — 잡히면 안 되는 것 (위 재현율의 짝)

| ID | 방향 | 요구사항 | 대표 케이스 (잠정) | 목표치 |
|---|---|---|---|---|
| BRK-LLM-30 | 오탐 | WHEN 입력이 **야구 도메인의 관용적 폭력·과장 표현**이면, THE 시스템 SHALL 통과시킨다 | `불펜이 또 방화했다` · `타선이 완전히 죽었다` · `살인적인 일정이네` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-23 의 짝) |
| BRK-LLM-31 | 오탐 | WHEN 입력이 **선수·감독·구단에 대한 성적/경기력 비판**이면(인격 비하 없음), THE 시스템 SHALL 통과시킨다 | `오늘 감독 용병술은 최악이었다` · `수비가 오늘 진짜 최악이었네` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-20 의 짝) |
| BRK-LLM-32 | 오탐 | WHEN 입력이 **팀·지역에 대한 일반적 응원·라이벌 표현**이면, THE 시스템 SHALL 통과시킨다 | `부산 원정 가서 이겨야 직성이 풀린다` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-21 의 짝) |
| BRK-LLM-33 | 오탐 | WHEN 입력이 **감탄·강조 관용구**이면, THE 시스템 SHALL 통과시킨다 | `미쳤다 이 타구` · `역대급 경기였다` · `말도 안 되는 수비` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-24 의 짝) |
| BRK-LLM-34 | 오탐 | WHEN 입력이 **패턴 검열을 통과한 정상 한국어 문장**이면, THE 시스템 SHALL 통과시킨다 | 정상 세트(패턴 통과분에서 추출) 전건 | **정상 세트 50문장(가정) 기준 축 A 신규 폐기 0건.** 기준선: validation 모듈이 자연스러운 한국어 30문장에서 신규 오탐 미발생 |
| BRK-LLM-35 | 오탐 | THE 시스템 SHALL 패턴 통과분 전체에 대한 **축 A 폐기율**을 상한 이하로 유지한다 | 패턴 통과 **판정 단위** 500개(가정) 표본 — 본문·댓글 모두 포함 | **축 A 폐기율 상한 = (잠정) 3%** — 확정 절차는 BRK-LLM-46. 분모는 **`abuse` 가 적용되는 전 단위**(본문+댓글). 상한을 넘으면 BRK-LLM-24 의 재현율 목표를 먼저 낮춘다 |

### 축 B 오탐 — 잡히면 안 되는 것 (구조적 위험이 큰 쪽)

| ID | 방향 | 요구사항 | 대표 케이스 (잠정) | 목표치 |
|---|---|---|---|---|
| BRK-LLM-40 | 오탐 | WHEN 게시글이 **경기 내용이 아니지만 선수·팀·야구 문화 맥락 안에 있으면**, THE 시스템 SHALL 통과시킨다 | `○○선수 인터뷰 태도 좋더라` · `내년 유니폼 디자인 어떻게 나올까` · `직관 갔는데 자리 뷰 좋았음` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-27 의 짝) |
| BRK-LLM-41 | 오탐 | WHEN 게시글이 **밈·드립·짤 형태의 커뮤니티 잡담**이면(야구 커뮤니티 맥락 안), THE 시스템 SHALL 통과시킨다 | `오늘 경기 요약.jpg` · `이 짤 아직도 웃김` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-27 의 짝) |
| BRK-LLM-42 | 오탐 | WHEN 입력이 **짧은 감탄·응원 한 줄**이면, THE 시스템 SHALL 통과시킨다 | `가자!!` · `오늘은 이긴다` · `ㅋㅋㅋㅋ` → 통과 | 대표 케이스 전건 통과. **짧다는 이유로 `offtopic` 판정하지 않는다** |
| BRK-LLM-43 | 오탐 | WHEN 입력이 **선수·구단 관련 상품·티켓에 대한 개인의 정보 공유나 질문**이면(판매 유도 없음), THE 시스템 SHALL 통과시킨다 | `굿즈 어디서 사는 게 제일 싸요?` · `티켓팅 몇 시에 열려요?` → 통과 | 대표 케이스 전건 통과 (BRK-LLM-25 의 짝). **판매 유도와 구매 문의를 가르는 경계** |
| BRK-LLM-44 | 오탐 | THE 시스템 SHALL 패턴 통과분 전체에 대한 **축 B 폐기율**을 상한 이하로 유지한다 | 패턴 통과 **판정 단위** 500개(가정) 표본 — 본문·댓글 모두 포함 | **축 B 폐기율 상한 = (잠정) 2%.** 분모는 **그 축이 적용되는 단위**로 센다 — `spam` 은 본문+댓글, `offtopic` 은 **본문 단위만**(BRK-LLM-19 로 댓글엔 적용되지 않으므로 댓글을 분모에 넣으면 폐기율이 인위적으로 낮게 나온다). 확정 절차는 BRK-LLM-46 |
| BRK-LLM-45 | 오탐 | WHEN 입력이 **정상 세트의 커뮤니티 글**이면, THE 시스템 SHALL 축 B 로 폐기하지 않는다 | 정상 세트 50문장(가정) — 야구 커뮤니티 실표본에서 추출 | **축 B 신규 폐기 0건.** 이 세트에는 BRK-LLM-40~43 유형(경기 외 잡담·밈·한 줄 응원·구매 문의)이 **반드시 포함**돼야 한다 — 안 넣으면 축 B 오탐은 측정 자체가 되지 않는다 |

### 목표치 확정 절차 (숫자는 전부 잠정치다)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| BRK-LLM-46 | 이벤트 | WHEN 첫 **shadow 실행**(BRK-LLM-48) 결과가 나오면, THE 시스템(팀) SHALL 폐기율 상한(BRK-LLM-35·44)을 그 실측치에 근거해 확정한다 | **실제 커뮤니티의 스팸·욕설 비율을 모른 채 상한을 정하면 "잡아야 할 것을 못 잡게 하는 족쇄"가 된다**(사용자 결정 근거). 확정 전까지 3%/2% 는 잠정치이며, 이 값 때문에 재현율 목표를 낮추는 결정을 **미리** 내리지 않는다. 근거가 `DRY_RUN` 이 아닌 이유: **DRY_RUN 은 전건 통과라 폐기율이 정의상 0** 이라 실측이 불가능하다 |
| BRK-LLM-47 | 이벤트 | WHEN `test-data` 가 실표본(`validation/pattern/success/`)에서 대표 케이스를 확정하고 `accuracy-tuner` 가 달성 가능한 조합을 찾으면, THE 시스템(팀) SHALL 재현율 목표(BRK-LLM-24·27)를 확정한다 | **재현율은 DRY_RUN 으로도 shadow 로도 측정되지 않는다** — 정답 라벨이 있어야 하기 때문이다. 그래서 폐기율(BRK-LLM-46)과 확정 경로가 다르다 |

> **양방향 규칙 확인**: 축 A 재현율 5건(20~24) ↔ 오탐 6건(30~35). 축 B 재현율 3건(25~27) ↔ 오탐 6건(40~45).
> **축 B 는 오탐 쪽을 더 두껍게** 깔았다 — 경계가 모호한 축이라 한쪽만 두면 커뮤니티 잡담이 대량으로 폐기된다.
>
> **우선순위 규칙**: 폐기율 상한(BRK-LLM-35·44)이 재현율 목표(BRK-LLM-24·27)보다 **우선한다.**
> 상한을 못 지키면 재현율 목표를 낮추는 방향으로 조정한다. 목표치 측정은 `accuracy-tuner`,
> 측정 세트 제작은 `test-data` 소관이다 — 이 문서는 목표만 정한다.

---

## 이미 기각된 것 (모듈 문서 "한계" 대조)

- **키보드 뷰 초성 매칭 → 대량 오탐으로 기각**(validation.md 한계). LLM 은 뷰 매칭을 쓰지 않으므로 이 실패 모드를
  직접 되살리지는 않는다. 다만 **교훈은 그대로 유효하다**: 재현율을 올리려고 판정 폭을 넓히면 오탐이 먼저 터진다.
  BRK-LLM-30~35·40~45 가 그 방어선이고, **축 B 가 정확히 그 위험을 다시 만드는 축**이다.
- **Shift 조합(`ㄲㅆㅃ`) 미지원**(validation.md 한계). 이 문서는 특정 우회 표기의 기계적 복원을 요구하지 않는다 —
  BRK-LLM-24 는 "**문맥상 명백한** 신조 비속어"로 좁혔고, 표기 복원 일반화를 계약으로 삼지 않는다. **충돌 없음.**
- **발음 로마자(`sibal`)의 사전 의존**(validation.md 한계). 이를 LLM 으로 일반화하고 싶은 유혹이 있으나,
  "**모든** 로마자 우회 표기를 탐지한다"는 검증 불가한 거짓 계약이므로 **쓰지 않았다.** 로마자 케이스는
  BRK-LLM-24 의 하위 케이스로만 다루고, 목표치는 실표본 대표 케이스 기준으로 한정한다. **충돌 없음.**
- **혼용 표기 오탐**(`역시b발라드` 류, validation.md 한계). 이건 패턴 단계의 오탐이라 **Bedrock 단계에 도달하지
  않는다**(폐기된 것은 LLM 입력이 아니다). 즉 이 단계는 패턴의 오탐을 **되돌려주지 못한다** — 아래 한계 참조.
- **축 B 는 기존 모듈 문서에 선례가 없다.** `validation` 모듈은 욕설 판정만 하고 주제 관련성 개념 자체가 없으므로
  기각된 전례도 없다. 다만 **축 B 는 사전·정규식으로 원리적으로 풀 수 없는 축**이라는 점을 못박아 둔다 —
  앞으로 "광고 키워드를 `banned_words.json` 에 넣어 거르자"는 제안이 나오면 그건 축 B 가 아니라 축 A 사전 확장이고,
  키워드 매칭은 `offtopic` 을 판정할 수 없으며 `spam` 에 대해서도 정상 글(BRK-LLM-43)을 대량 오탐한다.

## 알려진 한계 (이 기능 자체)
- **패턴 단계의 오탐은 여기서 복구되지 않는다.** 입력이 `validation/pattern/success/` 뿐이라, 사전이 잘못 폐기한
  정상 문장은 LLM 이 볼 기회조차 없다. 2단계 구조는 **미탐만 줄이고 오탐은 늘리는 방향으로만 작동**한다.
- **축 B 의 경계는 본질적으로 주관적이다.** "야구와 전혀 관련 없음"은 사람마다 판정이 갈린다(예: 선수 사생활,
  타 종목 이야기, 커뮤니티 공지·잡담). 목표치를 100% 로 잡을 수 없는 이유이며, **정답 세트 자체가 라벨러 판단에
  흔들린다** — 축 B 측정치는 축 A 측정치보다 신뢰구간이 넓다고 봐야 한다.
- **`offtopic` 은 댓글에 적용하지 않는다**(BRK-LLM-19). 따라서 **주제 무관 댓글은 걸러지지 않는다** — 광고 댓글만
  `spam` 으로 걸린다. 의도된 제외이며, 필요해지면 부모 글 본문을 컨텍스트로 함께 넘기는 방식이 대안(미도입).
- **축이 하나만 기록된다**(BRK-LLM-1c). 욕설+광고가 겹친 글은 `abuse` 로만 남아 **축 B 재현율 측정에서 누락**된다.
  ⚠️ **이 한계의 무게가 달라졌다** — 축 값이 이제 **폐기 범위까지 결정**하기 때문이다(PIPE-2SB-11b/11c).
  겹친 본문은 `abuse` 로 기록되어 **게시글 전체가 fail** 되므로, 축 판정이 틀리면 지표만 어긋나는 게 아니라
  **통과 댓글까지 함께 버려진다.** 두 축을 동시에 기록하는 스키마(배열)로 바꾸면 해소되지만 미도입.
- **배칭이 판정 품질을 떨어뜨릴 수 있다**(BRK-LLM-1, PIPE-2SB-71). 한 프롬프트에 여러 텍스트를 넣으면 모델이
  이웃 텍스트에 영향을 받거나 뒤쪽 항목을 성의 없이 판정할 수 있다. **배치 크기를 비용만 보고 키우면 안 되고**,
  `accuracy-tuner` 가 판정 품질과 함께 정해야 한다.
- **배칭은 실패 비용도 키운다.** BRK-LLM-13 의 "항목 수 불일치 → **호출 전체 재시도**"가 배치 크기만큼 비싸진다
  (10개 배치에서 한 항목이 어긋나면 10개를 다시 판정한다). 부분 응답을 짜맞추지 않기로 한 결정의 대가다.
- **판정이 완전히 재현되지 않는다.** temperature 0 을 고정해도 모델·프로파일 버전이 바뀌면 판정이 바뀐다.
- **모델·프롬프트 교체 후에도 기존 `_manifest` 마커는 유지된다**(사용자 확정). 교체 시점 이후 신규 글만 새 모델로
  판정되므로 **과거 데이터에는 서로 다른 기준의 판정이 섞여 있다.** `accuracy-tuner` 가 과거 산출물로 재현율·오탐을
  측정할 때는 `modelId`·`promptVersion`(BRK-LLM-18) 으로 **구간을 나눠 측정해야 하며, 전체 평균은 의미가 없다.**
- **4000자 초과 본문의 뒷부분은 판정되지 않는다**(BRK-LLM-10).
- **빈 본문 글은 `title` 로 판정된다**(PIPE-S3IO-32 / PIPE-2SB-8). 제목은 본문보다 짧고 압축적이라
  **같은 축이라도 판정 근거가 다르다** — 특히 `offtopic` 은 제목만 보고 주제를 가르게 된다. 표본의 **13.7%** 가
  이 경로이므로 축 B 지표를 볼 때 무시할 수 없는 모집단이며, `reasons[].unit=="title"` 로 갈라 봐야 한다.
  **반대로 `body` 가 있는 글의 제목은 여전히 판정되지 않는다**(PIPE-S3IO-35) — 제목에만 욕설이 있는 글은
  두 단계 모두 통과한다.
- **비용 방어선은 네 겹이고, 지금 가장 중요한 건 캐싱이다.** (1) **프롬프트 캐싱**(PIPE-2SB-64/65) — 판정 대상
  텍스트가 20~50토큰인데 시스템 프롬프트가 약 1,500토큰이라 **호출 비용의 대부분이 접두부**다. 캐싱이 빠지면
  비용이 몇 배로 뛴다. (2) 한 단위당 **호출 1회** — 축이 2개가 되어도 두 축을 한 프롬프트에서 함께 판정해야 한다
  (방법은 `accuracy-tuner` 소관). (3) **게시글 경계에서의 예산 확인**(PIPE-2SB-74) — 지금은 여유가 있어 놀고 있는
  방어선이지만, 크롤러가 댓글 수집을 붙이는 날 실제로 작동한다. (4) **멱등 skip**(PIPE-2SB-17).
  최종 방어선은 금액 상한(PIPE-2SB-60~63)이다. 06:00 이후에도 정제가 계속되는 결정(PIPE-2SB-35b)과 겹치면
  **하루 비용이 시간으로는 전혀 방어되지 않는다** — 금액 상한만이 실질적 제동이다.
- **예산으로 아예 시작되지 못한 게시글이 생길 수 있다**(PIPE-2SB-74). 게시글 단위 순차 처리라 **부분 판정된
  게시글은 없지만**(구 `judgedScope` 문제 소멸), 시작조차 못 한 게시글은 **본문·댓글 전부가 패턴 검열만 거친
  상태**로 남고 다음 배치가 이어받지 않는다. 지표를 볼 때 분모는 "처리 대상"이 아니라 **"실제 판정한 게시글"**
  이어야 한다(PIPE-2SB-44 의 미시작 건수 로그가 그 근거). **현재 물량(하루 약 $13)에서는 발생하지 않을
  전망이지만, 아래 크롤러 결합이 바뀌면 곧바로 상시 경로가 된다.**
- **⚠️ 축 B(`spam`)의 실질 커버리지가 크롤러에 묶여 있다.** 광고·스팸은 댓글에 붙는 비중이 큰데, 전체의 97%인
  dcinside 는 **댓글이 아예 수집되지 않는다**(크롤러 설계 — `community.py:263`, AJAX 미구현으로 항상 `[]`).
  즉 지금 축 B 는 사실상 **본문에서만** 작동한다. 축 B 재현율을 측정·해석할 때 이 모집단 편향을 빼놓으면 안 되고,
  크롤러가 AJAX 댓글 수집을 붙이는 순간 **측정 기준선이 통째로 바뀐다**(단위 수 약 12배, 비용 $13→$158).
- **모델 단가가 미확인 가정치다.** Sonnet 4 단가는 Anthropic 1P 단가를 파트너 단가로 **가정**한 값이다
  (Bedrock 페이지에서 확인된 건 Sonnet 5 프로모 $2/$10 뿐, AWS Pricing API 는 조직 SCP 로 차단).
  따라서 예산 추정과 `bedrock_spend_usd` 누적치는 **추정이지 청구액이 아니다** — 상세는 pipeline 문서 한계.

## 확정된 결정 (구 미해결 질문 — 해소분)
1. **판정 실패 시** = fail-open(통과). → BRK-LLM-15. 추적 공백은 `fallback` 플래그로 메움 → BRK-LLM-15b.
2. **데이터 범위** = **APAC 이내 유지**, `global.*` 금지. 단 **APAC ≠ 한국** — 8개 리전 라우팅. → BRK-LLM-6b/6c.
3. **모델** = 본문·댓글 **단일** `apac.*` Claude **Sonnet 4**. → BRK-LLM-5/6.
   (직전 이터레이션의 "댓글은 3 Haiku" 분리안은 **폐기** — 근거였던 비용 압박이 실측으로 사라졌고,
   모델을 나누면 본문·댓글 판정 기준이 갈려 축별 지표만 오염된다.)
4. **검열 범위** = **게시글 단위 순차 처리**(본문/댓글 구분 없음). 예산 확인은 **게시글 경계**에서.
   → PIPE-2SB-8/74. *(구 결정 "본문 전건 우선"은 폐기.)*
5. **비용 상한** = $30/밤(금액 기준, **소프트 상한** — 초과 폭은 최대 배치 1회분). → PIPE-2SB-60~63·74/75.
6. **모델·프롬프트 교체 시** = 기존 마커 유지, 교체 이후 신규 글만 새 기준. → 위 "알려진 한계".
7. **목표치(폐기율·재현율)** = 지금 숫자는 전부 **잠정치**이며 실측 후 확정. → BRK-LLM-46/47.
8. **인터페이스** = **이종 혼합 배칭**(`judge(items: list[{text, unit_kind}])`), 크기는 "게시글 N개분" 설정값.
   게시글 단위 순차 처리라 한 배치에 본문·댓글이 섞이므로 `unit_kind` 는 **항목별**이다. → BRK-LLM-1, PIPE-2SB-14/71.
   *(구 결정 "동종 단위 배칭"은 폐기 — 근거였던 "본문 전건 → 댓글" 순서가 사라졌다.)*
9. **폐기율 실측 경로** = `BEDROCK_SHADOW`(실제 호출 + 표본 + 마커 미기록). `DRY_RUN` 은 전건 통과라 폐기율
   측정에 쓸 수 없다. → BRK-LLM-48, BRK-LLM-46.

## 미해결 질문
- **없음.** 요구사항 승인에 필요한 결정은 전건 해소됐다.

## 승인 후 확정 항목 (실측·측정과 함께 결정)
> 아래는 **요구사항의 공백이 아니라, 실측 없이는 답할 수 없는 값·범위**다. 계약(케이스 클래스·확정 절차)은
> 승인됐고, 이 항목들은 `BRK-LLM-46/47` 의 확정 절차를 타고 결정된다. 결정 주체는 `accuracy-tuner`(측정)·
> `test-data`(세트)이며, **범위를 바꾸는 1번은 사용자 재확인이 필요하다.**

1. **`offtopic` 을 댓글에도 적용할까**(BRK-LLM-19, 현재 "본문만" 으로 계약).
   - A. 현행 유지(본문만) — 짧은 댓글 오탐 없음. 주제 무관 댓글은 안 걸러진다.
   - B. 댓글에도 적용 — 무관 댓글까지 정리되지만 `ㅋㅋㅋ`·`이건 좀` 같은 정상 반응이 대량 폐기될 위험이 크다.
   - C. 부모 글 본문을 컨텍스트로 함께 넘겨 댓글 판정 — 오탐은 줄지만 **토큰·비용이 댓글 수만큼 배가된다.**
     이번 예산 결정($30/밤) 이후로는 C 의 현실성이 크게 낮아졌다.
2. **정상 세트 규모·구성**(BRK-LLM-34/45, 현재 50문장 가정) — 특히 축 B 측정용 세트에 경기 외 잡담·밈·한 줄 응원·
   구매 문의가 얼마나 들어가야 하는지. 이게 빠지면 축 B 오탐은 측정 자체가 되지 않는다.
3. **`spam` 과 `offtopic` 을 끝까지 분리 운영할지** — 산출물엔 분리 기록하지만, 향후 재심·복구 정책을 두 값에
   다르게 적용할 계획이 있는지(없다면 축 B 를 단일 식별자로 합치는 편이 단순하다).
4. **축 B `spam` 목표를 본문 기준으로만 잡을지**(BRK-LLM-25/26). 전체의 97%인 dcinside 에 **댓글이 없어서**
   지금 축 B 는 사실상 본문 전용이다. A. 본문 기준으로만 목표를 잡고 댓글은 참고치(현실 반영) /
   B. 본문·댓글을 따로 잡는다(fmkorea 3% 표본만으로는 댓글 목표가 통계적으로 흔들린다).
