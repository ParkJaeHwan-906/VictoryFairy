# 퀴즈 이닝 기록 요구사항
> 상태: **승인됨(2026-08-12)** · 모듈: quiz(엔드포인트·서비스) + domain(`quiz_users_submit` 컬럼 추가) + common(`ErrorCode` 1건 신설) · 최종 수정: 2026-08-12
> **2026-08-12 개정 1차**: 초안의 Q1~Q8이 사용자 답변으로 **전부 확정**됐다(하단 "결정 근거"). `(가정)` 표시는 남아 있지 않다.
> **Q3이 초안을 뒤집었다** — TTL 만료·`/today` 미경유 제출은 "`inning`을 null로 저장하고 정상 처리"에서 **"403으로 거부"**로 바뀌었다. 이로써 Redis 키의 역할이 캐시에서 **제출 자격 증명**으로 승격되고, 제출 API의 계약이 실제로 바뀐다(새 상태코드 403 + 새 `ErrorCode`).
> 뒤집힌 요구사항 4건(QUIZ-INN-13·17·19·24)은 **폐기 표시로 남기고 번호를 재사용하지 않는다.** 대체 계약은 QUIZ-INN-29~40이다.
> **Q8도 A(항상 발급)로 확정됐다** — 미해결 질문은 없다. 문서 전체가 이미 A 전제로 쓰여 있어 본문 수정은 없다.

## 배경 / 목적
"그 문제를 **경기 몇 회에** 풀었는가"를 제출 기록에 남긴다. **용도는 통계·분석이다**(Q1 확정) — 점수·랭킹 산정의 입력이 아니므로 값 누락이 허용되고, 그래서 `inning`은 nullable이다.

**이 기능에는 성격이 다른 두 축이 한 경로에 얹혀 있다. 섞어 읽으면 계약을 잘못 구현한다.**

| 축 | 무엇 | 누락되면 |
|---|---|---|
| **이닝 값** | `games.current_inning`을 제출 기록에 복사 | **허용**(통계용, `inning IS NULL`로 남음) |
| **제출 자격 증명(submission ticket)** | `/today`로 그 문제를 받았다는 사실 + 그 시점부터 8분이라는 시한 | **거부**(403, 제출 자체가 성립하지 않음) |

Redis 키 하나가 두 역할을 겸한다 — **키의 존재 여부는 자격 증명이고, 키의 값은 이닝이다**(Q3 확정). 사용자 표현대로 "Redis 데이터를 하나의 보안 장치로" 쓴다: `/today`를 거치지 않은 제출은 부적절한 요청이므로 막고, 시한을 넘긴 문제는 **아무 흔적도 남기지 않아 미제출 상태로 되돌린다**(다음 `/today`에 다시 실려 나온다 → QUIZ-INN-32).

**원천 데이터는 한시적으로 비어 있다**: 이닝의 원천인 `games.current_inning`은 py-collector 미구현으로 현재 모든 행에서 null이고, `Quiz.game` FK 자체도 nullable이라 경기 미귀속 문제가 다수다. 사용자 확인 — **"곧 이닝 정보는 제공될 예정이고, 현재는 서비스 이전이라 문제 없다."** 즉 이 기능은 선(先)배선이며, 값이 채워지기 전에도 **자격 증명 축은 지금부터 실동작한다.**

## 범위
- 포함:
  - `quiz_users_submit.inning` 컬럼 신설 — domain
  - `GET /rt/quizzes/today` 응답 개수 상한(설정값, 기본 20)
  - `/today`가 귀속 경기 이닝을 함께 조회해 `(계정, 문제)` 키를 TTL 8분으로 발급
  - `POST /rt/quizzes/{quizId}/submit`이 그 키를 **자격 검사 + 이닝 값 출처**로 사용, 성공 시 **커밋 이후** 키 삭제
  - **키 부재 시 제출 거부 403 + `:common` `ErrorCode` 1건 신설**
  - Redis 장애 시 실패(500) 정책, 전 프로파일 Redis 필수화에 따른 설정 정정 지점
- 제외:
  - **초/말(`inning_half`)** — 사용자 명시. `games.inning_half`를 읽지도, 컬럼을 만들지도 않는다
  - **`/today`·제출·상세·이력 응답에 이닝 노출**(Q5 확정 — 미노출)
  - **이닝별 통계·조회 API** — 값을 쌓기만 하고 읽는 경로는 이번에 만들지 않는다
  - **기존 제출 기록의 이닝 백필** — 채울 원천 값이 존재하지 않는다
  - **`games` 쓰기 경로** — 채우는 주체는 py-collector이고 이 앱에는 쓰기 경로가 없다
  - **편성 수(`quiz.serve.daily-count`) 변경** — 이번 상한은 서빙 축이다(Q2 확정)
  - **채점·포인트·좋아요 로직 변경**(QUIZ-INN-26)
  - **제출 시한의 연장·복구 수단**(재발급 API, 유예 시간) — 시한을 넘긴 문제는 다음 `/today`로 다시 받는 것이 유일한 경로다

## 용어
| 용어 | 뜻 |
|---|---|
| **이닝(inning)** | `games.current_inning`의 값. 정의역은 1~11(`ck_games_current_inning`) |
| **귀속 경기** | `quizzes.game_id`가 가리키는 `games` 행. **null이 정상**(경기에 귀속되지 않은 문제) |
| **이닝 확보 실패** | 귀속 경기가 없거나(`quiz.game IS NULL`), 있어도 `games.current_inning IS NULL`인 상태. **현재는 100%가 이 상태다** |
| **제출 자격 증명(티켓)** | Redis 키 `quiz:inning:{userAccountId}:{quizId}`의 **존재**. `/today`가 발급하고 TTL 8분 뒤 자동 소멸한다 |
| **시한** | 티켓 TTL 8분. `/today` 재수신 때마다 다시 8분으로 갱신된다(QUIZ-INN-14) |
| **오늘 세트** | `quizzes.quiz_date = 오늘(KST)`인 문제 집합. 크기는 `quiz.serve.daily-count`(기본 10)가 정한다 |

## 대상 엔드포인트

| 메서드 | 경로 | 이 문서가 정의하는 것 |
|---|---|---|
| GET | `/rt/quizzes/today` | 기존 — **최대 20건 상한** + 이닝 조회 + **티켓 발급**(응답 필드 불변, Redis 저장 실패 시 500) |
| POST | `/rt/quizzes/{quizId}/submit` | 기존 — **티켓 검사(403 신설)** + 이닝 적재 + 커밋 후 티켓 삭제(응답 필드 불변) |
| GET | `/rt/quizzes/{quizId}` · `/rt/quizzes/submissions` · `POST /{quizId}/like` | **변경 없음** |

## 요구사항 (EARS)

> ID 규칙: `QUIZ-INN-<n>`. 기존 `QUIZ-LIKE-<n>`·`QUIZ-CHAT-<n>`·`QUIZ-CTAC-<n>`과 별개 계열이며 **번호는 재사용하지 않는다**(폐기된 4건도 자리를 비워 둔다).
> 인수 기준 앞의 `AC-INN-<요구사항번호>-<n>`은 `test-writer`가 1:1로 대응시킬 식별자다.

### A. 데이터 모델 (domain)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-1 | 유비쿼터스 | THE 시스템 SHALL 제출한 이닝을 `quiz_users_submit.inning` 한 컬럼에만 보관한다 | `AC-INN-1-1` `quiz_users_submit`에 `inning`(TINYINT) 컬럼이 존재한다. `AC-INN-1-2` **초/말을 담는 컬럼(`inning_half` 등)이 추가되지 않는다**(사용자 명시 제외 항목). `AC-INN-1-3` 이닝을 담는 다른 테이블·컬럼이 생기지 않는다 |
| QUIZ-INN-2 | 유비쿼터스 | THE 시스템 SHALL `inning`을 nullable로 두고 기본값(`@ColumnDefault`)을 두지 않는다 | `AC-INN-2-1` 컬럼 DDL이 `NULL` 허용이고 `DEFAULT`가 없다. `AC-INN-2-2` 컬럼 추가 후 **기존 제출 행의 값은 NULL**이며 어떤 숫자로도 채워지지 않는다. **근거(Q1 확정: 통계용이라 누락 허용): domain 기본은 `nullable = false`지만, "이미 행이 있는 테이블에 NOT NULL 컬럼 추가"는 `@ColumnDefault` + 자바 초기값이 강제되고(`UserAccount.point` 선례) 그 초기값(예: `0`)은 정의역 1~11 밖이라 "0회에 풀었다"는 거짓을 데이터로 남긴다.** "모름"은 NULL로 표현한다 |
| QUIZ-INN-3 | 유비쿼터스 | THE 시스템 SHALL `inning`에 1~11 범위의 값만 저장한다 | `AC-INN-3-1` 저장되는 값은 그 문제의 `games.current_inning`을 **그대로 복사한 값**이며 앱이 가공(보정·반올림·0 대체)하지 않는다. `AC-INN-3-2` 정의역은 `ck_games_current_inning`(1~11)과 동일하다. `AC-INN-3-3` **CHECK 제약은 이번에 걸지 않는다** — `ddl-auto=update`가 기존 테이블에 CHECK를 거는지가 환경마다 갈렸고(2026-08-11 prod/devdb 실측), 값의 출처가 이미 CHECK로 닫힌 컬럼이라 추가 방어의 이득이 1회성 DDL 비용보다 작다 |
| QUIZ-INN-4 | 예외 | IF 대상 환경의 `quiz_users_submit`에 `inning` 컬럼이 없으면, THEN THE 시스템 SHALL 제출 API를 정상 제공하지 못한다(배포 선행 조건 미충족) | `AC-INN-4-1` 컬럼 없는 환경의 제출 요청은 `ApiResponse` 래퍼 없는 500이다. `AC-INN-4-2` **quiz 앱의 prod `ddl-auto`는 `none`이라 quiz만 배포해서는 컬럼이 생기지 않는다** — 생성 주체는 `ddl-auto=update`인 `user` 앱의 기동이다. 배포 순서에 `user` 앱 재기동이 포함되어야 한다. `AC-INN-4-3` **nullable 컬럼 추가는 `update`가 반영하므로 1회성 DDL은 필요 없다**(`games.cancel_reason`·`games.current_inning` 선례 — UNIQUE·CHECK가 안 붙는 제약은 컬럼 추가에 해당 없음) |
| QUIZ-INN-5 | 유비쿼터스 | THE 시스템 SHALL `inning`을 제출 시점에 확정해 저장하고 이후 재계산·갱신하지 않는다 | `AC-INN-5-1` 제출 이후 그 경기의 이닝이 진행되어도 이미 저장된 행의 `inning`은 변하지 않는다. `AC-INN-5-2` 이력·상세 조회가 `inning`을 다시 계산하지 않는다. **근거: `QuizUserSubmit.isAnswer`가 스냅샷인 것과 같은 계열** |

### B. `/today` 응답 개수 상한

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-6 | 이벤트 | WHEN 사용자가 `GET /rt/quizzes/today`를 요청하면, THE 시스템 SHALL 최대 20건까지만 반환한다 | `AC-INN-6-1` 조건을 만족하는 문제가 25건이어도 응답 배열 길이는 20이다. `AC-INN-6-2` 20건 이하이면 상한이 응답을 바꾸지 않는다(19건 → 19건). `AC-INN-6-3` 상한 초과는 에러가 아니다 — 200이고 별도 표식(`hasMore` 등)도 추가되지 않는다 |
| QUIZ-INN-7 | 유비쿼터스 | THE 시스템 SHALL 이 상한을 하드코딩이 아니라 설정값으로 둔다 | `AC-INN-7-1` 설정 키 `quiz.serve.max-today-count`(env `QUIZ_SERVE_MAX_TODAY_COUNT`)가 존재하고 기본값이 20이다 — 기존 `quiz.serve.daily-count`/`QUIZ_SERVE_DAILY_COUNT`와 같은 구획·같은 표기. `AC-INN-7-2` 값을 5로 낮추면 같은 세트에서 5건만 반환된다. **근거(사용자 확정): "우선 20개로 하고 추후 조정하겠다" — 조정에 배포가 필요하면 안 된다** |
| QUIZ-INN-8 | 유비쿼터스 | THE 시스템 SHALL 상한을 **기존 정렬·필터가 모두 끝난 목록의 앞에서부터** 적용한다 | `AC-INN-8-1` 잘라내기 전 목록은 이미 ① 내가 푼 문제 제외 ② `preferredOnly` 필터 ③ 선호 우선 + 사용자별 고정 랜덤(+ id tie-break) 정렬이 끝난 상태다. `AC-INN-8-2` 같은 사용자·같은 세트라면 몇 번을 호출해도 잘려 나가는 문제가 동일하다. `AC-INN-8-3` 사용자마다 잘려 나가는 문제가 다를 수 있다(정렬이 사용자별 — 결함이 아니다). `AC-INN-8-4` `preferredOnly=true`로 5건이 남았다면 상한과 무관하게 5건이다 |
| QUIZ-INN-9 | 유비쿼터스 | THE 시스템 SHALL 이 상한이 편성 단계(`quiz.serve.daily-count`)를 바꾸지 않게 한다 | `AC-INN-9-1` 상한 도입 후에도 `QuizPublishService`가 채우는 그날 세트 크기는 `daily-count`(기본 10) 그대로다. `AC-INN-9-2` **기본 설정(편성 10 · 상한 20)에서는 상한이 발동하지 않는다**(알려진 결과 2). `AC-INN-9-3` 두 값이 서로를 검증하거나 강제하지 않는다(상한 < 편성 수여도 기동은 정상이며 그 경우 세트 뒷부분이 노출되지 않는다) |

### C. `/today` — 이닝 조회와 티켓 발급

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-10 | 이벤트 | WHEN `/today`가 문제 목록을 구성하면, THE 시스템 SHALL 각 문제의 귀속 경기(`quizzes.game_id`)의 `games.current_inning`을 함께 조회한다 | `AC-INN-10-1` 응답에 포함되는(=상한 적용 후 남은) 문제에 대해서만 조회한다 — 잘려 나간 문제는 조회하지 않는다. `AC-INN-10-2` `games.inning_half`는 읽지 않는다. `AC-INN-10-3` 이 조회가 `/today`의 기존 응답 필드·정렬·상태코드를 바꾸지 않는다 |
| QUIZ-INN-11 | 이벤트 | WHEN `/today`가 문제를 응답에 포함하면, THE 시스템 SHALL 그 `(요청자, 문제)` 티켓을 TTL 8분으로 Redis에 발급한다 | `AC-INN-11-1` **응답에 실린 모든 문제에 대해 키가 생긴다 — 이닝 확보 여부와 무관하다**(QUIZ-INN-40). 응답 항목이 20건이면 키도 20개다. `AC-INN-11-2` 각 키의 TTL이 480초 이하이고 만료 시각이 설정돼 있다(TTL 없는 영구 키가 생기지 않는다). `AC-INN-11-3` 다른 사용자가 같은 문제를 받으면 별도 키가 생긴다. `AC-INN-11-4` **상한으로 잘려 나간 문제에는 키가 생기지 않는다** — 받지 않은 문제는 제출할 수 없다 |
| QUIZ-INN-12 | 유비쿼터스 | THE 시스템 SHALL 티켓의 키를 `quiz:inning:{userAccountId}:{quizId}`, 값을 이닝의 문자열 표현으로 한다 | `AC-INN-12-1` 계정 7이 문제 23을 6회에 받았다면 키 `quiz:inning:7:23`, 값 `"6"`이다. `AC-INN-12-2` 값에 초/말·타임스탬프 등 다른 정보를 담지 않는다. `AC-INN-12-3` **접두사가 `quiz:inning:`으로 네임스페이스를 갖는다** — 같은 Redis 인스턴스를 `user` 앱의 `email:verify:*`와 quiz 실시간 채널(`realtime:events`)이 공유하므로 접두사 없는 키는 금지다. `AC-INN-12-4` 사용자 원문 표기(`user_account_id:quiz_id:inning`)와 대응한다 — 앞 둘이 키, 마지막이 값이다 |
| QUIZ-INN-13 | ~~예외~~ | ~~IF 이닝을 확보하지 못하면, THEN 티켓을 저장하지 않는다~~ | **폐기(2026-08-12) — QUIZ-INN-40으로 대체.** Q3(제출 거부) 확정으로 키가 자격 증명이 된 순간, 이 규칙은 **이닝 확보 실패가 100%인 현재 모든 제출을 403으로 막는다.** 저장 여부는 이닝 유무와 분리됐다. 번호 재사용 금지 |
| QUIZ-INN-14 | 이벤트 | WHEN 이미 티켓이 있는 문제를 같은 사용자가 `/today`로 다시 받으면, THE 시스템 SHALL 값을 덮어쓰고 TTL을 8분으로 갱신한다 | `AC-INN-14-1` 3회에 받은 뒤 5회에 다시 `/today`를 호출하면 값이 `"5"`가 되고 만료가 다시 8분 뒤로 밀린다. `AC-INN-14-2` 따라서 제출 기록에 남는 이닝은 "그 문제를 **마지막으로 받은** 이닝"이다. `AC-INN-14-3` **갱신은 시한 연장 수단이기도 하다** — 8분이 지나기 전에 `/today`를 다시 부르면 제출 자격이 유지된다(알려진 결과 1의 완화 경로) |
| QUIZ-INN-15 | 유비쿼터스 | THE 시스템 SHALL `/today` 응답의 필드 집합을 바꾸지 않는다 | `AC-INN-15-1` 항목 키 집합은 여전히 `id·type·question·difficulty·point·preferred·options`다 — `inning` 키가 없다. `AC-INN-15-2` 성공 상태코드는 200 그대로다(실패는 아래 QUIZ-INN-34가 추가한 500이 유일한 신설) |
| QUIZ-INN-16 | 유비쿼터스 | THE 시스템 SHALL 이닝 조회를 항목 수에 비례하지 않는 고정 SQL 수로 처리한다 | `AC-INN-16-1` `/today` 1요청에서 발생하는 SQL 건수가 응답 항목 1건일 때와 20건일 때 **같다**. `AC-INN-16-2` 문제마다 `games`를 단건 조회하는 구현(N+1)은 금지다. **선례·근거: `QuizOptionRepository`의 `quiz_id IN` 2쿼리 방식, 좋아요의 N+1 금지 계약(QUIZ-LIKE-35). `prod`는 `open-in-view: false`라 LAZY 접근은 `@Transactional` 안에서 끝나야 한다** |
| QUIZ-INN-17 | ~~예외~~ | ~~IF 티켓 저장이 실패하면, THEN `/today`를 200과 정상 목록으로 유지한다~~ | **폐기(2026-08-12) — QUIZ-INN-34로 대체.** Q4 확정으로 graceful degrade를 하지 않는다. 번호 재사용 금지 |

### D. 제출 — 자격 검사·이닝 적재·티켓 정리

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-18 | 이벤트 | WHEN 사용자가 `POST /rt/quizzes/{quizId}/submit`으로 제출하면, THE 시스템 SHALL `(요청자, 그 문제)` 티켓을 조회해 그 값을 제출 기록의 `inning`에 저장한다 | `AC-INN-18-1` `/today`에서 6회에 받은 문제를 제출하면 `quiz_users_submit.inning = 6` 행이 생긴다. `AC-INN-18-2` 조회 키는 QUIZ-INN-12의 형태이며 **경로·본문으로 이닝을 받지 않는다**(클라이언트가 이닝을 조작할 입력 경로가 없다). `AC-INN-18-3` 다른 사용자의 티켓을 읽지 않는다(계정이 키에 포함). `AC-INN-18-4` 값이 이닝 미상 마커면 `inning`은 NULL로 저장된다(QUIZ-INN-40) |
| QUIZ-INN-19 | ~~예외~~ | ~~IF 티켓이 없으면, THEN `inning`을 null로 저장하고 제출을 정상 처리한다~~ | **폐기(2026-08-12) — QUIZ-INN-29로 대체.** Q3 확정으로 정반대(403 거부)가 됐다. 번호 재사용 금지 |
| QUIZ-INN-20 | 유비쿼터스 | THE 시스템 SHALL 제출 처리 중 `games.current_inning`을 다시 조회하지 않는다 | `AC-INN-20-1` 제출 경로에서 `games`를 읽는 SQL이 발생하지 않는다. **근거: 기록하려는 값은 "문제를 받아 푼 시점의 이닝"이다. 제출 시점 재조회는 오래 붙들었다 낸 경우 다른 이닝을 사실인 것처럼 기록한다** |
| QUIZ-INN-21 | 이벤트 | WHEN 제출 트랜잭션이 커밋된 뒤에, THE 시스템 SHALL 해당 티켓을 삭제한다 | `AC-INN-21-1` 제출 성공 후 `quiz:inning:{계정}:{문제}` 키가 존재하지 않는다. `AC-INN-21-2` **삭제는 커밋 이후에 일어난다** — 삭제가 관측된 시점에는 제출 행이 이미 커밋돼 있다. `AC-INN-21-3` 같은 계정의 **다른 문제 티켓은 지워지지 않는다**(정확히 한 개만 삭제) — 한 문제를 냈다고 나머지 19문제의 자격이 사라지면 안 된다. **구현 수단(`TransactionSynchronization` afterCommit 계열)은 `spring-dev` 판단이며 계약은 "커밋 전에는 지워지지 않는다"는 관찰 가능한 순서뿐이다** |
| QUIZ-INN-22 | 예외 | IF 제출이 실패하거나 롤백되면(404·409·403·400, UNIQUE 위반 포함), THEN THE 시스템 SHALL 티켓을 삭제하지 않는다 | `AC-INN-22-1` 없는 보기 번호로 제출해 400을 받은 뒤에도 티켓이 남아 있고 TTL이 유지된다 — **곧바로 올바른 보기로 재시도하면 성공한다**(오타 한 번이 자격을 태우지 않는다). `AC-INN-22-2` 중복 제출 409는 티켓을 지우지 않는다. `AC-INN-22-3` 롤백된 요청 뒤 남은 티켓으로 재시도하면 같은 이닝이 기록된다(값이 유실되지 않는다) |
| QUIZ-INN-23 | 예외 | IF 커밋 후 티켓 삭제가 실패하면, THEN THE 시스템 SHALL 제출 응답을 200으로 유지하고 TTL 만료에 정리를 맡긴다 | `AC-INN-23-1` 삭제 실패 시에도 제출 결과(`correct·answer·myOption·earnedPoint·totalPoint`)가 정상 반환된다. `AC-INN-23-2` 삭제 실패가 제출 행을 되돌리지 않는다(이미 커밋됨 — 되돌릴 수단이 없다). `AC-INN-23-3` 남은 티켓은 최대 8분 뒤 자연 만료된다. **Q4(Redis 실패 = 500)의 유일한 예외이며 그 근거는 시점이다 — 커밋 전 실패는 되돌릴 수 있어 500이지만, 커밋 후 실패는 되돌릴 수 없어 500을 내면 "행은 저장됐는데 클라이언트는 실패로 안다"는 더 나쁜 상태가 된다**(선례: 채팅의 커밋 후 SSE 발행 fire-and-forget) |
| QUIZ-INN-24 | ~~예외~~ | ~~IF 제출 중 티켓 조회가 실패하면, THEN `inning`을 null로 저장하고 정상 처리한다~~ | **폐기(2026-08-12) — QUIZ-INN-37로 대체.** Q4 확정으로 500 실패가 됐다. 번호 재사용 금지 |
| QUIZ-INN-25 | 유비쿼터스 | THE 시스템 SHALL 제출·상세·이력 응답의 필드 집합을 바꾸지 않는다 | `AC-INN-25-1` 제출 성공 응답 키 집합은 여전히 `correct·answer·myOption·earnedPoint·totalPoint`다. `AC-INN-25-2` `GET /{quizId}`·`GET /submissions` 응답에도 `inning` 키가 없다. **근거(Q5 확정): 값을 적재까지만 요구했고 읽는 경로는 이 문서의 범위 밖이다** |
| QUIZ-INN-26 | 유비쿼터스 | THE 시스템 SHALL 이닝 기록이 채점·포인트·중복 제출 차단·좋아요에 영향을 주지 않게 한다 | `AC-INN-26-1` 같은 입력에서 `isAnswer`·`earnedPoint`·`users_account.point`가 이 기능 도입 전후로 동일하다. `AC-INN-26-2` `uk_quiz_users_submit_account_quiz`에 `inning`이 포함되지 않는다(이닝이 달라도 재제출은 여전히 409). `AC-INN-26-3` 계정 행 비관적 락(`findWithLockById`) 구간에 Redis 접근이 들어가지 않는다 — 티켓 검사·조회는 락보다 앞에서 끝난다(QUIZ-INN-33) |

### E. 제출 자격 증명 (Q3 확정 — 신설 계약)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-29 | 예외 | IF 제출 시점에 해당 티켓이 없으면(`/today` 미경유 또는 TTL 8분 경과), THEN THE 시스템 SHALL 403 `QUIZ_SUBMIT_NOT_ALLOWED`를 반환하고 제출을 처리하지 않는다 | `AC-INN-29-1` `/today`를 거치지 않고 곧바로 제출하면 403이다. `AC-INN-29-2` 티켓 발급 후 8분이 지나 제출하면 403이다. `AC-INN-29-3` **두 경우의 응답이 상태코드·본문 문자열까지 동일하다** — 클라이언트는 "안 받은 문제"와 "시한 초과"를 구분할 수 없다. `AC-INN-29-4` 상한(QUIZ-INN-6)으로 잘려 나가 받지 못한 문제도 같은 403이다 |
| QUIZ-INN-30 | 유비쿼터스 | THE 시스템 SHALL 이 거절에 `:common` `ErrorCode`의 403 신규 코드 1건을 사용한다 | `AC-INN-30-1` `ErrorCode`에 **`QUIZ_SUBMIT_NOT_ALLOWED(403, "오늘의 퀴즈로 받은 문제만 제한 시간 안에 제출할 수 있습니다.")`** 가 존재한다 — 기존 403 구획(`SELF_REPORT_NOT_ALLOWED`·`CHATROOM_TEAM_MISMATCH`·`QUIZ_LIKE_NOT_ALLOWED`) 자리다. `AC-INN-30-2` 응답은 프로젝트 공통 규약대로 `{"success":false,"data":null,"message":"<위 문구>"}`다. `AC-INN-30-3` 기존 코드(`QUIZ_NOT_FOUND`·`QUIZ_ALREADY_SUBMITTED`·`QUIZ_OPTION_NOT_FOUND`)를 이 용도로 재사용하지 않는다 |
| QUIZ-INN-31 | 유비쿼터스 | THE 시스템 SHALL 거절된 제출이 아무 흔적도 남기지 않게 한다 | `AC-INN-31-1` `quiz_users_submit`에 행이 생기지 않는다. `AC-INN-31-2` `users_account.point`가 변하지 않는다. `AC-INN-31-3` **Redis를 쓰지 않는다** — 없는 키를 지우려 하지도, 새로 만들지도 않는다. `AC-INN-31-4` 계정 행 락을 잡지 않는다(실패할 요청이 락을 잡고 시작하지 않는다 — 기존 검사 배치 원칙과 동일) |
| QUIZ-INN-32 | 이벤트 | WHEN 제출이 403으로 거절되면, THE 시스템 SHALL 그 문제를 이후 `/today` 응답에 다시 포함하고 새 티켓을 발급한다 | `AC-INN-32-1` 403을 받은 문제는 여전히 미제출 상태이므로 다음 `/today` 호출의 목록에 (정렬·상한 조건을 만족하는 한) 다시 실린다. `AC-INN-32-2` 그때 새 티켓이 그 시점의 이닝 값으로 발급되고 TTL이 8분으로 시작된다. `AC-INN-32-3` 그 티켓으로 제출하면 정상 200이다. **근거(사용자 원문): "TTL 만료 시 풀지 않은 문제로 추후 재사용 가능하도록 아무런 대응도 하지 않는다"** |
| QUIZ-INN-33 | 유비쿼터스 | THE 시스템 SHALL 제출 검증을 **404 → 409 → 403 → 400 → 계정 락·적립 → 저장** 순서로 수행한다 | `AC-INN-33-1` 존재하지 않거나 미편성인 문제는 티켓 유무와 무관하게 404다(은닉 정책 유지). `AC-INN-33-2` **이미 제출한 문제를 다시 제출하면 403이 아니라 409 `QUIZ_ALREADY_SUBMITTED`다** — 최초 제출이 티켓을 지웠으므로(QUIZ-INN-21) 재제출 시점엔 티켓이 없지만, 409 검사가 앞서므로 응답이 바뀌지 않는다. **이것이 기존 계약을 지키는 핵심 회귀 AC다.** `AC-INN-33-3` 티켓은 있으나 보기 번호가 없으면 400 `QUIZ_OPTION_NOT_FOUND`다(403이 400보다 앞). `AC-INN-33-4` 403으로 걸린 요청은 보기 조회·계정 락을 수행하지 않는다 |

### F. Redis 실패 정책 (Q4 확정)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-34 | 예외 | IF `/today` 처리 중 티켓 저장이 실패하면(연결 불가·타임아웃·명령 실패), THEN THE 시스템 SHALL 요청을 실패로 응답한다 | `AC-INN-34-1` Redis가 죽은 상태에서 `/today`는 200이 아니다(500). `AC-INN-34-2` 목록만 내려주고 티켓 발급을 건너뛰는 동작은 금지다 — 그 목록의 문제들은 어차피 전부 403이 되어 "받았는데 못 내는" 상태가 된다. `AC-INN-34-3` **근거(사용자): 현재 Redis와 MySQL이 같은 서버에 있어 Redis가 죽었으면 DB도 죽었을 상황이라 부분 성공을 만들 이유가 없다. 이 근거는 인프라 전제에 묶여 있다 — Redis를 별도 인스턴스로 분리하면 이 요구사항은 재검토 대상이다** |
| QUIZ-INN-35 | 유비쿼터스 | THE 시스템 SHALL `/today` 응답을 티켓 발급이 성공한 뒤에만 반환한다 | `AC-INN-35-1` 응답 본문에 실린 모든 문제는 응답 시점에 티켓이 존재한다(응답과 발급의 순서가 확정된다 — "동시에"가 아니라 "발급 후 응답"). `AC-INN-35-2` 일부만 발급된 상태에서 실패하면 응답은 500이고, **이미 쓰인 키는 지우지 않는다** — TTL 8분으로 자연 소멸하고, 재요청이 성공하면 QUIZ-INN-14의 덮어쓰기로 값·TTL이 갱신된다. `AC-INN-35-3` 부분 발급분이 남아도 무해하다 — 클라이언트는 목록을 받지 못했으므로 그 문제들을 제출하지 않으며, 설령 제출해도 정상 제출과 동일하게 처리된다(자격이 실제로 발급된 상태이므로) |
| QUIZ-INN-36 | 유비쿼터스 | THE 시스템 SHALL 한 번의 `/today` 처리에서 Redis 왕복 횟수를 항목 수에 비례하지 않게 한다 | `AC-INN-36-1` 응답 항목이 1건일 때와 20건일 때 **Redis 왕복(round-trip) 횟수가 같다** — 항목마다 `SET`을 한 번씩 보내 20회 왕복하는 구현은 금지다(파이프라인·다중 명령 등 수단은 `spring-dev` 판단). `AC-INN-36-2` TTL은 항목마다 개별로 걸린다(일괄 전송이 만료를 공유하게 만들지 않는다). **QUIZ-INN-16(SQL N+1 금지)과 같은 계열의 계약이며, 상한이 20이라 방치하면 왕복 20회가 응답 시간에 그대로 실린다** |
| QUIZ-INN-37 | 예외 | IF 제출 처리 중 티켓 조회가 실패하면(Redis 장애), THEN THE 시스템 SHALL 요청을 실패로 응답하고 제출을 저장하지 않는다 | `AC-INN-37-1` Redis가 죽은 상태의 제출은 200이 아니다(500). `AC-INN-37-2` `quiz_users_submit` 행이 생기지 않고 포인트도 적립되지 않는다(트랜잭션 롤백 또는 커밋 전 중단). `AC-INN-37-3` **조회 실패를 "티켓 없음"으로 해석해 403으로 응답하지 않는다** — 장애와 자격 미달은 다른 사실이고, 403으로 접으면 장애 중 사용자에게 "당신은 자격이 없다"고 잘못 알린다. `AC-INN-37-4` QUIZ-INN-23(커밋 후 삭제 실패)은 이 규칙의 예외다 |

### G. 환경·프로파일·배선 (Q7 확정)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-27 | 유비쿼터스 | THE 시스템 SHALL 이닝 티켓을 모든 프로파일에서 같은 저장소(Redis)로 처리한다 | `AC-INN-27-1` `dev`·`prod`·로컬·테스트에서 동일한 코드 경로를 탄다 — 프로파일에 따라 구현이 갈리지 않고 **인메모리 폴백을 두지 않는다**. `AC-INN-27-2` 선례는 `user` 모듈의 `RedisEmailVerificationStore`(프로파일 제한 없이 `StringRedisTemplate` 사용)다. `AC-INN-27-3` **근거(사용자 확인): "dev 환경도 Redis가 있으니 문제없이 진행 가능하다."** 폴백을 두면 dev·prod 동작이 갈려 TTL·만료·403 계약을 로컬에서 검증할 수 없고, 파드가 2개 이상인 환경에서는 `/today`와 제출이 서로 다른 파드로 가 자격 검사가 무작위로 깨진다 |
| QUIZ-INN-38 | 유비쿼터스 | THE 시스템 SHALL "quiz는 prod에서만 Redis를 쓴다"는 기존 전제를 문서·설정에서 정정한다 | `AC-INN-38-1` 정정 대상은 다음 셋이다 — ① `quiz/src/main/resources/application-dev.yaml`의 `management.health.redis.enabled: false`(근거였던 "dev는 Redis를 쓰지 않는다"가 무너졌으므로 헬스 인디케이터를 되살릴지 판단해야 한다) ② 같은 파일의 "dev 는 Redis 를 쓰지 않는다" 주석 ③ `application.yaml`의 `spring.data.redis` 주석("prod 전용", "prod 이외 프로파일은 이 값이 쓰이지 않는다"). `AC-INN-38-2` **`RedisPubSubPublisher`·`RealtimeRedisConfig`의 `@Profile("prod")`는 그대로 둔다** — 이 기능이 필요로 하는 것은 자동설정이 전 프로파일에 등록하는 `StringRedisTemplate`뿐이고, 기동 시 구독 연결을 맺는 리스너 컨테이너를 전 프로파일로 여는 것은 별개 결정이다(그 제한의 이유는 "Redis 없이 로컬을 못 띄운다"였다). `AC-INN-38-3` dev·로컬·CI 환경에 `REDIS_HOST`/`REDIS_PORT`가 실제로 주입되는지가 이 기능의 배포 선행 조건이다 |
| QUIZ-INN-28 | 예외 | IF 기동 시점에 Redis에 연결할 수 없으면, THEN THE 시스템 SHALL 애플리케이션 기동 자체는 중단하지 않는다 | `AC-INN-28-1` Redis 없이도 앱이 뜨고 `/health`·좋아요·상세·이력 등 Redis를 쓰지 않는 경로는 정상 동작한다. `AC-INN-28-2` 이때 `/today`와 제출만 500이다(QUIZ-INN-34·37). `AC-INN-28-3` **이 기능이 기동 시 연결을 맺는 빈(`RedisMessageListenerContainer` 같은)을 추가하지 않는다** — 추가하면 Redis 장애가 곧 파드 기동 실패가 되어 재시작 루프에 빠진다 |
| QUIZ-INN-39 | 유비쿼터스 | THE 시스템 SHALL 티켓이 남아 있는 것만으로 중복 제출이 성립하지 않게 한다 | `AC-INN-39-1` 삭제 실패로 티켓이 남은 문제에 다시 제출해도 409 `QUIZ_ALREADY_SUBMITTED`다. **근거: 티켓은 "제출해도 된다"는 필요조건일 뿐 충분조건이 아니다 — 검증 순서상 409가 403보다 앞이고(QUIZ-INN-33), 선검사를 동시에 통과해도 `uk_quiz_users_submit_account_quiz` UNIQUE가 최종 중재해 409로 접힌다.** `AC-INN-39-2` 따라서 티켓 삭제(QUIZ-INN-21)는 중복 방지 수단이 아니라 정리 작업이며, 실패해도 보안·정합성 결과가 없다 |

### H. 이닝 값과 자격의 분리 (Q3 확정의 연쇄)

| ID | 유형 | 요구사항 | 인수 기준 |
|---|---|---|---|
| QUIZ-INN-40 | 예외 | IF 문제의 이닝을 확보하지 못하면(귀속 경기 없음 또는 `current_inning IS NULL`), THEN THE 시스템 SHALL 티켓을 **이닝 미상 값으로 발급**한다 **(가정 — Q8)** | `AC-INN-40-1` `quiz.game IS NULL`인 문제도, `current_inning IS NULL`인 문제도 **키는 생긴다**(제출 자격이 살아 있다). `AC-INN-40-2` 그 값은 이닝 미상 마커 `"-"`이며 1~11 어떤 값과도 구분된다. `AC-INN-40-3` 그 티켓으로 제출하면 200이고 `inning IS NULL`인 행이 생긴다. `AC-INN-40-4` **이 요구사항이 없으면 이닝 확보 실패가 100%인 현재 모든 제출이 403이 되어 서비스가 멈춘다** — QUIZ-INN-13(폐기)이 그 상태였다. `AC-INN-40-5` 마커 문자는 값이 비어 있음을 Redis 응답만으로 판정할 수 있어야 한다는 요구를 만족하면 되며, `"-"`는 그 확정값이다 |

## 알려진 결과 (택하면 따라오는 것 — 결함이 아니다)
1. **사용자는 `/today`로 받은 문제를 8분 안에 제출해야 한다.** 20문제를 한 번에 받아 8분을 넘기면 남은 문제는 403으로 거부되고 미제출로 되돌아간다(QUIZ-INN-29·32). **사용자가 이 함의를 인지한 상태에서 내린 결정이다.** 완화 경로는 하나뿐이다 — 만료 전에 `/today`를 다시 부르면 TTL이 8분으로 갱신된다(QUIZ-INN-14). FE가 주기적으로 `/today`를 갱신하는 화면 구조라면 사실상 문제가 되지 않고, 한 번 받아 오래 붙드는 구조라면 후반부 문제가 대량 403이 된다.
2. **기본 설정에서 20 상한은 발동하지 않는다.** 편성이 10건(`daily-count`)이므로 상한은 세트가 20을 넘게 되는 날에만 의미를 갖는다. "우선 20, 추후 조정"이라 설정값으로 뒀다(QUIZ-INN-7).
3. **당분간 모든 제출의 `inning`은 NULL이다.** 원천(py-collector)이 곧 제공될 예정이며 현재는 서비스 이전이라 무해하다(사용자 확인). **다만 자격 증명 축은 지금부터 실동작하므로, 이닝이 비어 있는 동안에도 403 계약은 그대로 적용된다.**
4. **Redis 장애 = 퀴즈 조회·제출 전면 장애다**(QUIZ-INN-34·37). 의도된 결과이며 근거는 "Redis와 MySQL이 같은 서버"라는 현재 인프라다. **Redis를 분리하는 순간 이 결정의 전제가 사라지므로 재검토 트리거로 기록해 둔다.**
5. **티켓이 남아도 중복 제출은 뚫리지 않는다**(QUIZ-INN-39). 삭제 실패분은 최대 8분 뒤 사라지고 그 사이 재제출은 409다.
6. **`/today`를 여러 기기·탭에서 받으면 마지막 호출이 이긴다.** 키가 `(계정, 문제)` 하나라 값과 TTL이 마지막 호출로 덮인다(QUIZ-INN-14). 채팅 구독의 last-one-wins 축출과 같은 방향의 성질이다.
7. **로컬 개발이 원격 Redis에 키를 쓴다.** `.env`의 `REDIS_HOST`는 원격이며 로컬 도커의 `victoryfairy-redis`가 아니다(quiz 모듈의 명시된 함정). 개발자끼리 같은 인스턴스를 쓰면 `quiz:inning:*`가 섞인다 — 계정·문제 id가 키에 있어 충돌은 없지만 관측이 헷갈린다.
8. **제출 실패(400·409)는 자격을 태우지 않는다**(QUIZ-INN-22). 보기 번호를 잘못 보낸 요청이 티켓을 소모하면 오타 한 번으로 문제를 못 풀게 된다.

## 미해결 질문

없음 — Q1~Q8이 모두 확정됐다(아래 "결정 근거").

## 결정 근거 (해소된 질문 — 다시 논의하지 않기 위해)
1. **Q1 용도 → 통계용.** 누락 허용이라 `inning`은 nullable(QUIZ-INN-2). 사용자 부연: "곧 이닝 정보는 제공될 예정이고, 현재는 서비스 이전이라 문제 없다" — 원천 부재는 알려진 한시적 상태이며 이 기능은 선배선이다.
2. **Q2 상한 → 서빙 상한만 20, 편성은 `daily-count`(10) 유지.** "우선 20개로 하고 추후 조정하겠다"이므로 하드코딩이 아니라 설정값 `quiz.serve.max-today-count`(기본 20)다(QUIZ-INN-7). 편성 축을 건드리지 않아 "전원 동일 데일리 세트(레이팅 공정성)" 원칙에 영향이 없다.
3. **Q3 시한 초과·미경유 제출 → 403 거부.** 사용자 원문: "`/today`를 거치지 않고 제출하는 것은 부적절한 요청입니다. Redis 데이터를 하나의 보안 장치로 여기려 합니다. TTL 만료 시 풀지 않은 문제로 추후 재사용 가능하도록 아무런 대응도 하지 않습니다."
   - **키의 의미가 승격됐다**: 캐시(있으면 좋고 없어도 그만) → **자격 증명**(없으면 제출 불가). 이 문서의 상단 표가 그 분리를 고정한다.
   - **상태코드 403**: "인증은 됐지만 이 자원에 이 동작을 할 자격이 없다"이므로 `QUIZ_LIKE_NOT_ALLOWED`와 같은 구획이다. 404(은닉)는 부적절하다 — 문제의 존재는 이미 `/today`로 알려 준 사실이고, 400은 "요청이 잘못됐다"라 시한 초과를 설명하지 못한다.
   - **거절은 흔적을 남기지 않는다**(QUIZ-INN-31) → 그 문제는 미제출로 남아 다음 `/today`에 다시 실린다(QUIZ-INN-32). 이것이 사용자가 말한 "추후 재사용"의 구현 형태다.
   - **검증 순서 404 → 409 → 403 → 400 고정**(QUIZ-INN-33): 최초 제출이 티켓을 지우므로 재제출 시점엔 티켓이 없다. 403 검사를 409보다 앞에 두면 **재제출 응답이 409에서 403으로 바뀌어 기존 계약이 깨진다.** 순서가 곧 계약이다.
4. **Q4 Redis 장애 → 500 실패.** 사용자 근거: "현재 Redis와 MySQL이 같은 서버에 있어서 의도한 동작" — Redis가 죽었으면 DB도 죽었을 상황이라 부분 성공을 만들 이유가 없다. 부수 효과로 `/today`의 순서가 확정된다: **발급 성공 후 응답**(QUIZ-INN-35). **인프라 전제에 묶인 결정이므로 Redis 분리 시 재검토한다.**
5. **Q5 노출 → 미노출.** `/today`·제출 응답 모두 필드 집합 불변(QUIZ-INN-15·25). FE 계약이 바뀌지 않는다.
6. **Q6 재수신 → 덮어쓰고 TTL 갱신**(QUIZ-INN-14). 기록되는 값은 "마지막으로 받은 이닝"이고, 부수 효과로 갱신이 시한 연장 수단이 된다(알려진 결과 1의 유일한 완화 경로).
7. **Q7 프로파일 → 전 프로파일 Redis 필수, 폴백 없음.** 사용자 확인: "dev 환경도 Redis가 있으니 문제없이 진행 가능합니다." 정정이 필요한 설정 지점은 QUIZ-INN-38에 열거했고, **실시간 fan-out 빈의 `@Profile("prod")`는 건드리지 않는다**(다른 축이다).
8. **Q8 티켓 발급 범위 → 이닝 확보 여부와 무관하게 항상 발급(A).** 자격 증명 축과 이닝 값 축을 분리한다(QUIZ-INN-40) — 이닝을 못 구한 문제도 티켓을 받아 제출이 200으로 통과하고 `inning`만 NULL로 남는다. 근거: Q1의 "현재는 서비스 이전이라 문제 없다"는 **이닝 값 누락**에 대한 양해이지 제출 전면 차단의 승인이 아니며, B를 택하면 이닝 확보 실패율이 100%인 현재 모든 제출이 403이 되어 배포 자체가 불가능하다. 사용자 승인 2026-08-12.

## 기존 정책과의 충돌 / 계약 성립 제약 (구현 지시가 아니라 지켜야 할 사실)
1. **제출 API의 계약이 실제로 바뀐다 — 이번 개정의 가장 큰 파급.** `docs/api/quiz.md`의 `POST /rt/quizzes/{quizId}/submit` 절에 **403이 신설**되고 실패 목록이 404/409/400 세 가지에서 네 가지가 된다. 기존 세 응답의 조건·문구는 불변이다. 구현 후 문서 갱신은 `api-documenter` 소관이다.
2. **재제출 409를 지키는 것이 검증 순서의 유일한 목적이다.** 이미 제출한 문제는 `/today`에 실리지 않아 티켓도 없다 — 순서를 잘못 잡으면 조용히 403으로 바뀐다. 기존 회귀 테스트(`QuizSubmitServiceTest`·`QuizSubmissionControllerHistoryTest` 계열)가 이 경계를 지키는지 확인해야 한다(`test-writer` 소관, AC-INN-33-2).
3. **원천 데이터 부재와 자격 증명이 충돌한다** — 이 문서가 발견해 QUIZ-INN-40으로 막은 지점이다. **Q8이 A로 확정돼 해소됐다**(티켓은 항상 발급). 다시 뒤집지 말 것 — "이닝을 모르면 티켓을 안 준다"로 되돌리는 순간 이닝 확보 실패율이 100%인 현재 모든 제출이 403이 되어 서비스가 멈춘다.
4. **`quiz.serve.daily-count`는 편성 설정이지 서빙 설정이 아니다.** 현재 `/today`에는 개수 상한이 전혀 없다. 상한은 서빙 축에 새로 생기는 개념이므로 별도 키가 필요하다 — 같은 키를 재사용하면 편성과 서빙이 한 값에 묶인다.
5. **`/today`의 정렬 계약과 충돌하지 않게 자를 것.** 정렬은 "선호 우선 + 사용자별 고정 랜덤(splitmix64) + id tie-break"이고 **부분집합 안정성**이 회귀 테스트 3건으로 고정돼 있다. 상한은 그 정렬 결과를 앞에서 자르는 연산이어야 한다.
6. **`QuizRepository`의 `@EntityGraph`에 `game`이 없다**(현재 `quizType·team·opponentTeam·player`). "응답·판정이 읽는 연관과 1:1"이 그 javadoc의 원칙이므로 축이 하나 늘어야 하고, 형태는 항목 수에 비례하지 않아야 한다(QUIZ-INN-16). `open-in-view: false`라 LAZY 접근은 `@Transactional` 안에서 끝나야 한다.
7. **quiz 모듈의 Redis 전제가 바뀐다**(QUIZ-INN-38). 기존 Redis 빈은 전부 `prod` 전용이고 dev yaml에 "dev는 Redis를 쓰지 않는다"가 문서화돼 있다. **다만 실시간 fan-out의 프로파일 제한을 함께 걷어내면 안 된다** — 그 제한의 이유(기동 시 구독 연결)는 이 기능과 무관하게 여전히 유효하다.
8. **스키마를 만드는 앱과 쓰는 앱이 다르다.** quiz의 prod `ddl-auto`는 `none`이라 컬럼은 `user` 앱 기동이 만든다(QUIZ-INN-4). 배포는 **quiz 앱 배포 + user 앱 재기동**이 한 묶음이며, nullable 컬럼이라 1회성 DDL은 불필요하다.
9. **`quiz_users_submit`은 이미 행이 있는 테이블이다.** NOT NULL 추가는 `@ColumnDefault` + 자바 초기값이 강제되는데 이닝 정의역(1~11)에는 "값 없음"을 표현할 숫자가 없다 — QUIZ-INN-2가 컨벤션 기본값의 예외를 택한 근거다.
10. **`:common` `ErrorCode` 1건 신설이 계약 성립 조건이다.** `QUIZ_SUBMIT_NOT_ALLOWED(403, ...)`가 없으면 QUIZ-INN-29/30/31/32가 성립하지 않는다. `common`은 spring 의존 없이 status를 int로 보관하는 enum이라 추가는 한 줄이다.
11. **테스트 환경 제약**: 저장소 전체에 H2/Testcontainers/embedded Redis가 없어 **Redis 계열은 슬라이스·목 기반 검증만 가능**하다. TTL 실제 만료·커밋 순서(afterCommit)·파이프라인 왕복 수의 라운드트립 검증은 이 제약 아래서 설계되어야 한다(`test-writer` 소관).
