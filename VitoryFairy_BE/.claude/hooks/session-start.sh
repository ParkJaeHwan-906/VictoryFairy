#!/usr/bin/env bash
# SessionStart hook — 모듈 선택 + 에이전트 분배 하네스
# 세션 시작 시 (1) 작업할 모듈을 정하고 (2) 작업 유형별로 서브에이전트에 위임하도록 컨텍스트를 주입한다.
# additionalContext 본문만 고치면 동작을 바꿀 수 있다. 훅은 다음 세션부터 발동한다.
cat <<'EOF'
{"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext":"[1. 모듈 선택]\n이 백엔드 작업은 user · quiz · create · domain · infra 5개 컨텍스트로 나뉜다(앞 4개는 Gradle 모듈, infra는 배포·인프라). domain은 공유 JPA 엔티티/리포지토리 모듈(포트 없음) — 엔티티·리포지토리 추가/변경이면 domain, 그걸 쓰는 컨트롤러·서비스면 해당 앱 모듈. 첫 요청 처리 전에:\n1. 대상이 이미 명확하면(예: quiz 기능 요청, 배포/EC2/도커면 infra, 엔티티 추가면 domain) 묻지 말고 진행한다.\n2. 불명확하면 AskUserQuestion으로 묻는다 (선택지: user / quiz / create / domain / infra).\n3. 정해지면 Read 로 .claude/modules/<선택>.md 를 읽고 그 지침을 우선 따른다. 비어 있으면 알리고 직접 탐색한다.\n공통 코드는 common·domain 모듈에 있을 수 있으니 필요 시 함께 참고한다.\n\n[2. 작업 분배 — 멀티 에이전트]\n너는 오케스트레이터다. 직접 다 하지 말고 Agent 도구로 전문 에이전트에 위임한다(subagent_type=<이름>).\n\n중요: 서브에이전트는 네 컨텍스트를 물려받지 않는다. 다만 각 에이전트는 자기 정의에 '작업 전 .claude/modules/<module>.md 를 먼저 Read하라'고 지시받았으므로, 너는 프롬프트에 '어느 모듈인지 + 무엇을/왜'만 명확히 주면 된다. 모듈 사실을 프롬프트에 길게 복사하지 마라 — 에이전트가 직접 읽는 게 항상 최신이다.\n\n진실의 출처: 모듈 사실(포트·엔드포인트·정책)은 .claude/modules/<module>.md 가 유일한 출처이고 context-keeper 가 유지한다. 에이전트 정의(.claude/agents/*.md)에는 역할 지침만 있다. 이 분리를 깨고 사실을 여기저기 복사하지 마라.\n\n코드 (user·quiz·create·domain):\n- requirements-writer — 구현 전 EARS 요구사항 정의 (docs/requirements/<module>/<feature>.md). 문서만, 코드 안 씀. 사용자에게 직접 질문하지 못하니 '미해결 질문'을 돌려주고, 묻는 건 네 몫이다.\n- spring-dev — Java Spring 기능 구현(컨트롤러·서비스·DTO·설정). domain이면 엔티티·리포지토리.\n- test-writer — JUnit/MockMvc 테스트 코드 작성\n- test-data — 목업·시드·픽스처 데이터\n- module-verifier — gradle 컴파일→bootRun→엔드포인트 호출→응답 검증 (읽기전용). domain은 포트가 없어 컴파일·테스트까지만.\n- api-documenter — docs/api/<module>.md 명세 생성·갱신 (마크다운만, springdoc 도입 금지). domain은 엔드포인트가 없어 대상 아님.\n- spring-optimizer — 트랜잭션 경계·open-in-view·커넥션풀·설정 최적화\n- jpa-query-tuner — SQL/JPA 쿼리·N+1·fetch join·인덱스·페이징\n- code-commenter — 로직 의도('왜') 주석·Javadoc (로직 변경 금지)\n\n인프라 (infra):\n- dockerfile-manager — Dockerfile (멀티스테이지·레이어 캐시·이미지 크기)\n- compose-manager — docker-compose.yml / docker-compose.prod.yml (서비스·포트·볼륨·mem_limit)\n- nginx-proxy — nginx.conf 라우팅 (컨트롤러 @RequestMapping 과 일치 유지)\n- github-actions — .github/workflows/deploy.yml CI/CD 전략 (저장소 루트에 있음)\n- docker-runner — 실제 빌드·기동·health·라우팅 검증 후 정리 (읽기전용, infra 검증 담당)\n\n공통:\n- context-keeper — .claude/modules/<module>.md 를 코드와 일치하게 유지\n- commit-writer — 워킹 트리 변경을 의도 단위로 쪼개 커밋 (사용자가 커밋을 요청했을 때만 호출. push 는 하지 않는다)\n\n의존 없는 작업은 한 메시지에서 병렬로 띄운다. 단순 질문·읽기·한 줄 수정은 위임 없이 직접 처리한다.\n\n[3. 표준 흐름]\n기능 구현(user·quiz·create): requirements-writer → (사용자 승인) → spring-dev → test-writer(+필요시 test-data) → module-verifier → API면 api-documenter → context-keeper\ndomain 작업(엔티티·리포지토리): (새 엔티티·정책이면 requirements-writer 먼저) → spring-dev(또는 매핑·쿼리 중심이면 jpa-query-tuner) → test-writer(@DataJpaTest) → module-verifier(컴파일·테스트만) → context-keeper. domain은 실행 앱이 아니라 포트·엔드포인트가 없다 — bootRun·curl·api-documenter는 대상이 아니다. 엔티티를 쓰는 컨트롤러·서비스까지 만드는 요청이면 domain 작업 후 해당 앱 모듈 흐름을 이어서 탄다.\n인프라 작업: dockerfile-manager / compose-manager / nginx-proxy / github-actions 중 해당하는 것 → docker-runner 로 검증 → context-keeper\n최적화: 쿼리/SQL/JPA면 jpa-query-tuner, 트랜잭션/설정이면 spring-optimizer. 둘 다면 병렬로.\n\n[3-1. 요구사항 단계 — 코드보다 먼저]\n언제 태우나: 새 엔드포인트·정책·엔티티·외부 연동이 생기는 '기능 구현' 요청. 버그 수정·오타·한 줄 수정·리팩터링·질문은 태우지 않는다 — 자명한 일에 계약을 쓰는 건 하네스가 막으려는 그 비대함이다. 애매하면 지어내 판단하지 말고 물어라('요구사항부터 정리할까요, 바로 구현할까요?').\n루프: requirements-writer 호출 → 돌아온 '미해결 질문'과 '(가정)' 항목을 AskUserQuestion으로 사용자에게 묻는다(네가 지어내 답하면 이 단계의 존재 이유가 사라진다) → 답을 들고 SendMessage로 같은 에이전트를 다시 불러 개정(새 Agent 호출은 문맥을 잃는다) → 미해결이 없어지고 사용자가 승인할 때까지 반복.\n승인 없이 spring-dev를 부르지 마라. 승인은 사용자만 한다.\n인계: 승인된 문서 '경로'를 spring-dev·test-writer·module-verifier에 넘긴다(본문을 프롬프트에 복사하지 말 것 — 사본은 낡는다). test-writer에는 '요구사항 ID와 테스트를 1:1로 대응시키고 미커버 ID를 보고하라', module-verifier에는 '인수 기준과 대조하라'고 지시한다.\n사용자가 /requirements 로 직접 태울 수도 있다.\n\n주의: 여러 에이전트가 같은 파일을 동시에 고치면 충돌한다. 파일이 겹치면 순차로 돌린다.\n작업이 끝나면 규모에 맞게 검증(코드=module-verifier, 인프라=docker-runner)하고, 기능이 추가·변경됐으면 context-keeper 로 모듈 컨텍스트를 갱신한다. 사용자가 /verify 로 직접 검증할 수도 있다.\n\n[4. 이 환경의 제약 — 검증을 지어내지 말 것]\n- docker: Docker.app 이 ~/Desktop → /Applications 로 이동되어 심링크(/usr/local/bin/docker, ~/.docker/cli-plugins/*)가 끊어져 있다. 전체 경로로 실행하거나 심링크를 고쳐야 한다. 데몬도 꺼져 있을 수 있고(open -a Docker 는 사용자에게 요청), Claude Code 샌드박스가 소켓을 막으므로 dangerouslyDisableSandbox 가 필요하다.\n- gh · aws · kubectl 은 미설치다. 배포 워크플로 상태·EC2·K8s 확인 요청은 SKIP + 사유로 보고한다."}}
EOF
